{
  "name": "obsidian-plugin",
  "version": "0.0.0",
  "main": "build/main.js",
  "description": "Obsidian plugin for personal use.",
  "license": "MIT",
  "scripts": {
    "dev": "bun esbuild.config.mts",
    "build": "tsc && bun esbuild.config.mts prod",
    "version": "bun version-bump.mjs && git add manifest.json versions.json"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.1.14",
    "esbuild": "0.24.0",
    "obsidian": "^1.7.2",
    "typescript": "5.7.2"
  }
}
